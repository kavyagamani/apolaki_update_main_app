# Frontend environment variables for Netlify production build
# These are used by Vite at build time
# At runtime on Netlify, the frontend will use relative paths /api/*
# which will be routed through netlify.toml redirects to /.netlify/functions/handler

VITE_API_URL=/api
