apiVersion: v1
kind: Service
metadata:
  name: {{ include "apolaki-frontend.fullname" . }}
  labels:
    {{- include "apolaki-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    {{- include "apolaki-frontend.selectorLabels" . | nindent 4 }}
